<Window x:Class="SnipTool.UI.LibraryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library"
        Width="1120"
        Height="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackground}">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Library" FontSize="18" FontWeight="SemiBold" />
                <TextBox x:Name="SearchBox" Grid.Row="1" Margin="0,12,0,10" Height="34" />
                <TreeView x:Name="FolderTree" Grid.Row="2" />
                <Button x:Name="OpenRootButton" Grid.Row="3" Content="Open root folder" Margin="0,12,0,0" Style="{StaticResource SecondaryButton}" />
            </Grid>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Captures" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock x:Name="CountText" Style="{StaticResource MutedText}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="OpenButton" Content="Open" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" />
                        <Button x:Name="ShowInFolderButton" Content="Show in folder" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" />
                        <Button x:Name="EditButton" Content="Edit" Margin="0,0,8,0" />
                        <Button x:Name="RefreshButton" Content="Refresh" Style="{StaticResource SecondaryButton}" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1">
                    <ListBox x:Name="CaptureList"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <Setter Property="Margin" Value="0,0,12,12" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <Border x:Name="Border"
                                                    Background="{DynamicResource SurfaceBackground}"
                                                    BorderBrush="{DynamicResource AppBorder}"
                                                    BorderThickness="1"
                                                    CornerRadius="12"
                                                    Padding="10">
                                                <ContentPresenter />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Accent}" />
                                                    <Setter TargetName="Border" Property="BorderThickness" Value="2" />
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Accent}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="220">
                                    <Border Background="{DynamicResource AppBackground}" BorderBrush="{DynamicResource AppBorder}" BorderThickness="1" CornerRadius="8">
                                        <Grid>
                                            <Image Source="{Binding Thumbnail}" Height="140" Stretch="UniformToFill" />
                                            <Border x:Name="VideoBadge"
                                                    Background="#AA000000"
                                                    CornerRadius="6"
                                                    Padding="6,2"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Bottom"
                                                    Margin="8"
                                                    Visibility="Collapsed">
                                                <TextBlock Text="Video" Foreground="White" FontSize="12" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                    <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" Margin="0,8,0,0" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding FolderName}" Style="{StaticResource MutedText}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsVideo}" Value="True">
                                        <Setter TargetName="VideoBadge" Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock x:Name="EmptyStateText"
                               Text="No captures found"
                               Style="{StaticResource MutedText}"
                               FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="Collapsed" />
                </Grid>

                <TextBlock Grid.Row="2" Text="Double-click a capture to open the editor." Style="{StaticResource MutedText}" Margin="0,12,0,0" />
            </Grid>
        </Border>
    </Grid>
</Window>
